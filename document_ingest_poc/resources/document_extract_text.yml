resources:
  pipelines:
    document_extract_text:
      name: document_extract_text
      catalog: ${var.catalog}
      schema: ${var.gold_schema}
      serverless: true
      root_path: "../src/document_transformations_gold/transformations"

      libraries:
        - glob:
            include: ../src/document_transformations_gold/transformations//**

      environment:
        dependencies:
          - --editable ${workspace.file_path}
